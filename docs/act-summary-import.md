# Legacy Tracker JSON import removed

The manual Tracker summary JSON importer was removed in beta.147. BYAKUGAN's
temporary replacement is the opt-in visible-page Tracker sync in Settings.

This compatibility document overwrites stale copies during ZIP-based source
updates and does not describe an active feature.
